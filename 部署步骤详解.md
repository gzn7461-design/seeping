# CommentHub 私有化部署 —— 详细步骤指南

> 本文档面向初学者，每一步都写清楚命令和操作方式，照着做即可完成部署。

---

## 目录

- [第一步：准备服务器](#第一步准备服务器)
- [第二步：安装 Node.js 和 pnpm](#第二步安装-nodejs-和-pnpm)
- [第三步：安装 PostgreSQL 数据库](#第三步安装-postgresql-数据库)
- [第四步：获取项目代码](#第四步获取项目代码)
- [第五步：安装项目依赖](#第五步安装项目依赖)
- [第六步：配置环境变量](#第六步配置环境变量)
- [第七步：创建数据库表结构](#第七步创建数据库表结构)
- [第八步：构建项目](#第八步构建项目)
- [第九步：启动服务](#第九步启动服务)
- [第十步：验证部署](#第十步验证部署)
- [第十一步：配置 Nginx 反向代理（可选但推荐）](#第十一步配置-nginx-反向代理可选但推荐)
- [第十二步：配置 HTTPS 证书（可选但推荐）](#第十二步配置-https-证书可选但推荐)
- [第十三步：配置防火墙](#第十三步配置防火墙)
- [第十四步：设置开机自启](#第十四步设置开机自启)
- [附录：常见问题](#附录常见问题)

---

## 第一步：准备服务器

### 1.1 购买服务器

推荐使用以下云服务商：
- **阿里云** — 云服务器 ECS
- **腾讯云** — 轻量应用服务器 / CVM
- **华为云** — 弹性云服务器
- **AWS** — EC2

### 1.2 服务器配置要求

| 配置项 | 最低要求 | 推荐配置 |
|--------|---------|---------|
| CPU | 2核 | 4核 |
| 内存 | 4GB | 8GB |
| 磁盘 | 20GB | 50GB |
| 带宽 | 5Mbps | 10Mbps |
| 操作系统 | Ubuntu 22.04 | Ubuntu 24.04 |

### 1.3 登录服务器

**Windows 用户**（使用 PuTTY 或 PowerShell）：

```powershell
# PowerShell 方式
ssh root@你的服务器IP地址
```

**Mac / Linux 用户**（使用终端）：

```bash
ssh root@你的服务器IP地址
```

> 示例：`ssh root@123.456.789.0`
>
> 首次登录会提示输入密码，输入密码时屏幕不会显示字符，这是正常的。

### 1.4 更新系统包

登录成功后，先更新系统软件包：

```bash
# 更新软件包列表
apt update

# 升级所有已安装的软件包
apt upgrade -y

# 安装常用工具
apt install -y curl wget git vim unzip
```

---

## 第二步：安装 Node.js 和 pnpm

### 2.1 安装 Node.js 24.x

使用 nvm（Node Version Manager）安装：

```bash
# 步骤1：下载并安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.0/install.sh | bash

# 步骤2：让 nvm 命令生效
source ~/.bashrc

# 步骤3：安装 Node.js 24
nvm install 24

# 步骤4：使用 Node.js 24
nvm use 24

# 步骤5：验证安装（应显示 v24.x.x）
node -v

# 步骤6：验证 npm（应显示 10.x.x）
npm -v
```

如果看到类似 `v24.0.0` 的输出，说明 Node.js 安装成功。

### 2.2 安装 pnpm

```bash
# 步骤1：全局安装 pnpm
npm install -g pnpm@latest

# 步骤2：验证安装（应显示 9.x.x）
pnpm -v
```

如果看到类似 `9.15.0` 的输出，说明 pnpm 安装成功。

---

## 第三步：安装 PostgreSQL 数据库

> 你可以选择以下两种方案之一：
> - **方案A**：使用 Supabase 云服务（推荐，无需自己维护数据库）
> - **方案B**：自建 PostgreSQL（数据完全在自己服务器上）

### 方案A：使用 Supabase 云服务（推荐）

**适合不想自己维护数据库的用户。**

#### 步骤1：注册 Supabase 账号

1. 打开浏览器访问 https://supabase.com
2. 点击右上角 "Start your project" 按钮
3. 使用 GitHub 账号登录，或者用邮箱注册
4. 登录后进入控制台

#### 步骤2：创建项目

1. 点击 "New project" 按钮
2. 填写以下信息：
   - **Name**：`commenthub`（或你喜欢的名称）
   - **Database Password**：设置一个复杂的密码（**务必记下来**）
   - **Region**：选择离你最近的区域（国内用户选 Singapore 或 Tokyo）
   - **Pricing Plan**：选择 Free 免费计划即可
3. 点击 "Create new project"
4. 等待约 2-3 分钟，项目创建完成

#### 步骤3：获取连接信息

1. 在项目左侧菜单中，点击 **Project Settings**（齿轮图标）
2. 点击 **API** 标签页
3. 找到以下三个值，**复制保存**：
   - **Project URL**（格式如 `https://xxxxx.supabase.co`）
   - **anon public**（一串长字符串，以 `eyJ` 开头）
   - **service_role key**（一串长字符串，以 `eyJ` 开头，需点击 "Reveal" 显示）

> **注意**：service_role key 有最高权限，**不要泄露**给任何人。

### 方案B：自建 PostgreSQL

**适合对数据安全要求高、希望完全自控的用户。**

#### 步骤1：安装 PostgreSQL

```bash
# 安装 PostgreSQL
apt install -y postgresql postgresql-contrib

# 查看安装状态
systemctl status postgresql

# 如果没有启动，手动启动
systemctl start postgresql

# 设置开机自启
systemctl enable postgresql
```

#### 步骤2：创建数据库和用户

```bash
# 切换到 postgres 系统用户
sudo -u postgres psql
```

此时你会进入 PostgreSQL 的命令行界面（提示符变为 `postgres=#`）。

逐条执行以下 SQL 命令（每条命令后的 `;` 不能省略）：

```sql
-- 创建数据库
CREATE DATABASE commenthub;

-- 创建用户，将 'your_password' 替换为你的密码
CREATE USER commenthub_user WITH PASSWORD 'sunyes123@2026';

-- 赋予数据库权限
GRANT ALL PRIVILEGES ON DATABASE commenthub TO commenthub_user;

-- 连接到 commenthub 数据库
\c commenthub

-- 赋予 schema 权限
GRANT ALL ON SCHEMA public TO commenthub_user;

-- 退出 PostgreSQL
\q
```

#### 步骤3：配置远程访问（可选）

如果应用和数据库在同一台服务器，跳过此步骤。

```bash
# 编辑 PostgreSQL 配置文件
vim /etc/postgresql/*/main/postgresql.conf
```

找到 `#listen_addresses = 'localhost'` 这一行，修改为：

```
listen_addresses = '*'
```

保存退出（按 `Esc`，输入 `:wq`，回车）。

```bash
# 重启 PostgreSQL
systemctl restart postgresql
```

---

## 第四步：获取项目代码

### 方式1：从 Git 仓库克隆（推荐）

```bash
# 步骤1：创建一个目录存放项目
mkdir -p /opt/commenthub
cd /opt/commenthub

# 步骤2：克隆代码（将 URL 替换为你的仓库地址）
git clone https://github.com/你的用户名/commenthub.git .

# 注意：最后的 . 表示克隆到当前目录
```

### 方式2：上传代码包

```bash
# 步骤1：在本地电脑上将代码打包
# 在项目目录下执行：
tar -czf commenthub.tar.gz --exclude='node_modules' --exclude='.next' --exclude='.git' .

# 步骤2：将压缩包上传到服务器
# 在本地电脑执行（将 IP 替换为你的服务器 IP）：
scp commenthub.tar.gz root@你的服务器IP:/opt/commenthub/

# 步骤3：在服务器上解压
cd /opt/commenthub
tar -xzf commenthub.tar.gz
```

---

## 第五步：安装项目依赖

```bash
# 进入项目目录
cd /opt/commenthub

# 安装所有依赖
pnpm install
```

等待安装完成，看到类似 `Done in 30.5s` 的提示，说明依赖安装成功。

---

## 第六步：配置环境变量

### 6.1 创建环境变量文件

```bash
# 创建 .env.local 文件
nano .env.local
```

### 6.2 写入配置

根据你的数据库选择，填入对应的配置：

#### 如果使用 Supabase（方案A）

```env
# ===== Supabase 配置 =====
# 将下面的值替换为第三步中获取到的值
SUPABASE_URL=https://你的项目ID.supabase.co
SUPABASE_ANON_KEY=你的anon_key
SUPABASE_SERVICE_ROLE_KEY=你的service_role_key

# ===== 应用配置 =====
# 替换为你的域名或服务器IP
NEXT_PUBLIC_APP_URL=http://你的服务器IP:5000
```

#### 如果使用自建 PostgreSQL（方案B）

```env
# ===== 自建 PostgreSQL 配置 =====
# 将下面的值替换为你创建的数据库信息
DATABASE_URL=postgresql://commenthub_user:你的密码@localhost:5432/commenthub

# ===== 应用配置 =====
# 替换为你的域名或服务器IP
NEXT_PUBLIC_APP_URL=http://你的服务器IP:5000
```

### 6.3 保存文件

- 按 `Ctrl + O` 保存文件
- 按 `Enter` 确认文件名
- 按 `Ctrl + X` 退出编辑器

---

## 第七步：创建数据库表结构

### 如果使用 Supabase（方案A）

1. 打开浏览器，登录 Supabase 控制台
2. 进入你的项目
3. 点击左侧菜单的 **SQL Editor**（SQL 编辑器）
4. 点击 **New Query**（新建查询）
5. 将以下 SQL 全部复制粘贴到编辑器中：

```sql
-- ==================== 股票列表 ====================
CREATE TABLE IF NOT EXISTS stock_list (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  stock_code VARCHAR(20) NOT NULL,
  stock_name VARCHAR(50) NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS stock_list_code_idx ON stock_list(stock_code);

-- ==================== 评论模板 ====================
CREATE TABLE IF NOT EXISTS comment_templates (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  title VARCHAR(200) NOT NULL,
  content TEXT NOT NULL,
  category VARCHAR(50) NOT NULL DEFAULT 'general',
  tags TEXT,
  stock_code VARCHAR(20),
  stock_name VARCHAR(50),
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS comment_templates_category_idx ON comment_templates(category);
CREATE INDEX IF NOT EXISTS comment_templates_stock_code_idx ON comment_templates(stock_code);

-- ==================== 发布任务 ====================
CREATE TABLE IF NOT EXISTS publish_tasks (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  template_id VARCHAR(36) REFERENCES comment_templates(id) ON DELETE SET NULL,
  content TEXT NOT NULL,
  target_url TEXT NOT NULL,
  target_platform VARCHAR(50) NOT NULL DEFAULT 'eastmoney',
  status VARCHAR(20) NOT NULL DEFAULT 'pending',
  stock_code VARCHAR(20),
  stock_name VARCHAR(50),
  scheduled_at TIMESTAMP WITH TIME ZONE,
  published_at TIMESTAMP WITH TIME ZONE,
  error_message TEXT,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS publish_tasks_status_idx ON publish_tasks(status);
CREATE INDEX IF NOT EXISTS publish_tasks_scheduled_at_idx ON publish_tasks(scheduled_at);

-- ==================== 股吧评论（核心表） ====================
CREATE TABLE IF NOT EXISTS stock_comments (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  stock_code VARCHAR(20) NOT NULL,
  stock_name VARCHAR(50) NOT NULL,
  title TEXT,
  username VARCHAR(100) NOT NULL,
  comment_content TEXT NOT NULL,
  comment_time TIMESTAMP WITH TIME ZONE NOT NULL,
  source_url TEXT,
  read_count INTEGER DEFAULT 0,
  reply_count INTEGER DEFAULT 0,
  sentiment VARCHAR(20) NOT NULL DEFAULT 'neutral',
  sentiment_score VARCHAR(10),
  ai_analysis TEXT,
  has_sensitive_words VARCHAR(10) DEFAULT 'false',
  sensitive_words TEXT,
  is_processed VARCHAR(10) DEFAULT 'false',
  collected_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS stock_comments_stock_code_idx ON stock_comments(stock_code);
CREATE INDEX IF NOT EXISTS stock_comments_sentiment_idx ON stock_comments(sentiment);
CREATE INDEX IF NOT EXISTS stock_comments_comment_time_idx ON stock_comments(comment_time);
CREATE INDEX IF NOT EXISTS stock_comments_has_sensitive_words_idx ON stock_comments(has_sensitive_words);
CREATE INDEX IF NOT EXISTS stock_comments_is_processed_idx ON stock_comments(is_processed);

-- ==================== 预警配置 ====================
CREATE TABLE IF NOT EXISTS alert_configs (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  stock_code VARCHAR(20) NOT NULL,
  stock_name VARCHAR(50) NOT NULL,
  negative_threshold VARCHAR(10) NOT NULL DEFAULT '30',
  check_interval VARCHAR(10) NOT NULL DEFAULT '30',
  alert_types TEXT DEFAULT '["negative","sensitive"]',
  wecom_webhook TEXT NOT NULL,
  is_active VARCHAR(10) NOT NULL DEFAULT 'true',
  daily_push_enabled VARCHAR(5) DEFAULT 'false',
  daily_push_time VARCHAR(5) DEFAULT NULL,
  check_negative VARCHAR(5) DEFAULT 'true',
  check_sensitive VARCHAR(5) DEFAULT 'true',
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS alert_configs_stock_code_idx ON alert_configs(stock_code);
CREATE INDEX IF NOT EXISTS alert_configs_is_active_idx ON alert_configs(is_active);

-- ==================== 预警记录 ====================
CREATE TABLE IF NOT EXISTS alert_records (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  config_id VARCHAR(36) REFERENCES alert_configs(id) ON DELETE CASCADE,
  stock_code VARCHAR(20) NOT NULL,
  stock_name VARCHAR(50) NOT NULL,
  alert_type VARCHAR(50) NOT NULL,
  threshold VARCHAR(10) NOT NULL,
  actual_value VARCHAR(10) NOT NULL,
  message TEXT NOT NULL,
  sent_at TIMESTAMP WITH TIME ZONE NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS alert_records_config_id_idx ON alert_records(config_id);
CREATE INDEX IF NOT EXISTS alert_records_stock_code_idx ON alert_records(stock_code);

-- ==================== 自动采集配置 ====================
CREATE TABLE IF NOT EXISTS auto_collect_configs (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  stock_code VARCHAR(20) NOT NULL,
  stock_name VARCHAR(50) NOT NULL,
  collect_interval VARCHAR(20) NOT NULL DEFAULT 'daily',
  collect_time VARCHAR(10),
  page_size VARCHAR(10) NOT NULL DEFAULT '50',
  is_active VARCHAR(10) NOT NULL DEFAULT 'true',
  last_collected_at TIMESTAMP WITH TIME ZONE,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS auto_collect_configs_stock_code_idx ON auto_collect_configs(stock_code);
CREATE INDEX IF NOT EXISTS auto_collect_configs_is_active_idx ON auto_collect_configs(is_active);

-- ==================== 敏感字库 ====================
CREATE TABLE IF NOT EXISTS sensitive_words (
  id VARCHAR(36) PRIMARY KEY DEFAULT gen_random_uuid(),
  word VARCHAR(100) NOT NULL,
  category VARCHAR(50) NOT NULL DEFAULT 'general',
  level VARCHAR(20) NOT NULL DEFAULT 'medium',
  is_active VARCHAR(10) NOT NULL DEFAULT 'true',
  created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS sensitive_words_word_idx ON sensitive_words(word);
CREATE INDEX IF NOT EXISTS sensitive_words_category_idx ON sensitive_words(category);
CREATE INDEX IF NOT EXISTS sensitive_words_is_active_idx ON sensitive_words(is_active);

-- ==================== 插入默认敏感字 ====================
INSERT INTO sensitive_words (word, category, level) VALUES
('骗子', 'spam', 'high'),
('垃圾', 'spam', 'medium'),
('有毒', 'violent', 'medium'),
('崩盘', 'general', 'high'),
('割肉', 'general', 'medium')
ON CONFLICT DO NOTHING;
```

6. 点击 **Run**（运行）按钮，等待执行完成
7. 在左侧菜单点击 **Table Editor**，应该能看到 8 张表

### 如果使用自建 PostgreSQL（方案B）

```bash
# 步骤1：将 SQL 保存到文件
cat > /tmp/create_tables.sql << 'SQLEOF'
-- 将上面 SQL 编辑器中的全部内容粘贴到这里
SQLEOF

# 注意：上面的命令只是模板，请将上面的完整 SQL 粘贴到引号之间

# 步骤2：执行 SQL 文件
psql -U commenthub_user -d commenthub -f /tmp/create_tables.sql
```

---

## 第八步：构建项目

```bash
# 进入项目目录
cd /opt/commenthub

# 执行构建
pnpm build
```

> 构建过程需要 1-3 分钟，取决于服务器性能。
>
> 如果看到类似以下输出，说明构建成功：
> ```
> ✓ Compiled successfully
> ✓ Collecting page data ...
> ✓ Generating static pages ...
> ✓ Finalizing page optimization ...
> ```

---

## 第九步：启动服务

### 方式1：直接启动（测试用）

```bash
cd /opt/commenthub
pnpm start
```

> 这种方式启动后，如果关闭终端，服务就会停止。
> 按 `Ctrl + C` 停止服务。

### 方式2：使用 PM2 进程管理（推荐，生产环境用）

```bash
# 步骤1：安装 PM2
npm install -g pm2

# 步骤2：启动服务
pm2 start pnpm --name "commenthub" -- start

# 步骤3：保存进程列表（重启服务器后自动恢复）
pm2 save

# 步骤4：设置开机自启
pm2 startup
```

> 执行 `pm2 startup` 后，会提示你运行一行命令，复制并执行它。

---

## 第十步：验证部署

### 10.1 检查服务状态

```bash
# 查看 PM2 管理的服务状态
pm2 status
```

应该看到类似输出：
```
┌─────┬────────────┬──────────┬──────┬───────────┬──────────┐
│ id  │ name       │ status   │ cpu  │ memory    │ uptime   │
├─────┼────────────┼──────────┼──────┼───────────┼──────────┤
│ 0   │ commenthub │ online   │ 0.1% │ 98.4 MB   │ 2m       │
└─────┴────────────┴──────────┴──────┴───────────┴──────────┘
```

### 10.2 访问页面

打开浏览器，访问：`http://你的服务器IP:5000`

> 示例：`http://123.456.789.0:5000`

如果看到 CommentHub 的界面，说明部署成功！

### 10.3 检查日志

```bash
# 查看最近 50 行日志
pm2 logs commenthub --lines 50
```

确保没有红色报错信息。

---

## 第十一步：配置 Nginx 反向代理（可选但推荐）

> 配置 Nginx 后，可以通过域名访问，而不用在 URL 后面加端口号 `:5000`。

### 11.1 安装 Nginx

```bash
apt install -y nginx
```

### 11.2 创建 Nginx 配置

```bash
# 创建配置文件
nano /etc/nginx/sites-available/commenthub
```

在编辑器中粘贴以下内容（将 `your-domain.com` 替换为你的域名或服务器 IP）：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 上传文件大小限制（最大 50MB）
    client_max_body_size 50m;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # 静态资源缓存（提高加载速度）
    location /_next/static/ {
        proxy_pass http://localhost:5000;
        expires 365d;
        add_header Cache-Control "public, immutable";
    }
}
```

保存退出（`Ctrl + O` → `Enter` → `Ctrl + X`）。

### 11.3 启用配置

```bash
# 创建软链接（启用站点）
ln -s /etc/nginx/sites-available/commenthub /etc/nginx/sites-enabled/

# 删除默认站点
rm -f /etc/nginx/sites-enabled/default

# 测试配置是否正确
nginx -t

# 如果看到 "test is successful"，重启 Nginx
systemctl restart nginx
```

### 11.4 更新环境变量

```bash
# 编辑环境变量
nano /opt/commenthub/.env.local
```

修改 `NEXT_PUBLIC_APP_URL` 为：

```
NEXT_PUBLIC_APP_URL=http://your-domain.com
```

重启服务：

```bash
pm2 restart commenthub
```

现在可以通过 `http://your-domain.com` 访问了。

---

## 第十二步：配置 HTTPS 证书（可选但推荐）

> 配置 HTTPS 后，浏览器地址栏会显示"安全锁"图标，数据传输加密。

### 12.1 安装 Certbot

```bash
apt install -y certbot python3-certbot-nginx
```

### 12.2 获取 SSL 证书

```bash
# 将 your-domain.com 替换为你的域名
certbot --nginx -d your-domain.com
```

> 注意：**必须使用域名**才能申请证书，不能直接用 IP 地址。

按照提示操作：
1. 输入邮箱地址（用于接收证书到期通知）
2. 同意服务条款（输入 `A` 回车）
3. 选择是否分享邮箱（可选 `N`）

### 12.3 验证自动续期

```bash
certbot renew --dry-run
```

如果看到 "Congratulations" 说明自动续期配置成功。

### 12.4 更新环境变量

```bash
nano /opt/commenthub/.env.local
```

修改 `NEXT_PUBLIC_APP_URL` 为：

```
NEXT_PUBLIC_APP_URL=https://your-domain.com
```

重启服务：

```bash
pm2 restart commenthub
```

现在可以通过 `https://your-domain.com` 安全访问了。

---

## 第十三步：配置防火墙

### 使用 UFW（Ubuntu 默认防火墙）

```bash
# 安装 UFW
apt install -y ufw

# 设置默认策略：拒绝所有入站，允许所有出站
ufw default deny incoming
ufw default allow outgoing

# 开放 SSH 端口（必须，否则你会连不上服务器）
ufw allow 22/tcp

# 开放 HTTP 和 HTTPS 端口
ufw allow 80/tcp
ufw allow 443/tcp

# 如果不用 Nginx 代理，也要开放应用端口
ufw allow 5000/tcp

# 启用防火墙
ufw --force enable

# 查看状态
ufw status
```

应该看到：
```
Status: active

To                         Action      From
--                         ------      ----
22/tcp                     ALLOW       Anywhere
80/tcp                     ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
5000/tcp                   ALLOW       Anywhere
```

---

## 第十四步：设置开机自启

### 14.1 确保 PM2 的开机自启已设置

```bash
# 如果之前已经执行过 pm2 startup，可以跳过此步
pm2 startup
```

### 14.2 验证自启设置

```bash
# 查看 PM2 的启动脚本
systemctl list-unit-files | grep pm2
```

应该看到 `pm2-root.service` 状态为 `enabled`。

---

## 附录：常见问题

### Q1：服务启动失败，端口被占用

```bash
# 查看 5000 端口被哪个进程占用
lsof -i :5000

# 或者使用 ss 命令
ss -tlnp | grep 5000

# 杀死占用端口的进程（将 PID 替换为实际进程号）
kill -9 PID
```

### Q2：数据库连接失败

```bash
# 检查环境变量是否正确加载
cat /opt/commenthub/.env.local | grep -E "SUPABASE_URL|DATABASE_URL"

# 测试 Supabase 连接
curl -s -X GET "$SUPABASE_URL/rest/v1/stock_comments?select=count" \
  -H "apikey: $SUPABASE_ANON_KEY" \
  -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"

# 测试自建 PostgreSQL 连接
psql -U commenthub_user -d commenthub -h localhost -c "SELECT 1"
```

### Q3：页面访问报 502 Bad Gateway

```bash
# 检查服务是否运行
pm2 status

# 检查 Nginx 配置
nginx -t

# 查看 Nginx 错误日志
tail -f /var/log/nginx/error.log

# 常见原因：Nginx 的 proxy_pass 端口和服务端口不一致
# 检查 /etc/nginx/sites-available/commenthub 中的端口号
```

### Q4：上传 Excel 文件失败

```bash
# 检查 Nginx 是否限制了上传大小
# 在配置中添加：client_max_body_size 50m;

# 重启 Nginx
systemctl restart nginx

# 检查 Excel 文件格式
# 必须包含列：作者、评论内容、时间
# 可选列：标题、链接、阅读、回复
```

### Q5：AI 分析不生效

```bash
# 检查服务器能否访问外网
curl -s --max-time 5 https://api.coze.cn

# 查看应用日志中的 AI 分析报错
pm2 logs commenthub --lines 100 | grep -i "analyze\|llm\|ai\|error"
```

### Q6：如何更新到最新版本

```bash
# 进入项目目录
cd /opt/commenthub

# 拉取最新代码
git pull

# 安装新依赖
pnpm install

# 重新构建
pnpm build

# 重启服务
pm2 restart commenthub

# 检查日志
pm2 logs commenthub --lines 50
```

### Q7：如何备份和恢复数据库

**备份：**

```bash
# 自建 PostgreSQL
pg_dump -U commenthub_user commenthub > /backup/commenthub_$(date +%Y%m%d).sql

# Supabase：在 Supabase 控制台 → Database → Backups 中下载备份
```

**恢复：**

```bash
# 自建 PostgreSQL
psql -U commenthub_user commenthub < /backup/commenthub_20250101.sql
```

### Q8：忘记服务器密码怎么办？

1. 登录云服务商控制台
2. 找到你的服务器实例
3. 点击"重置密码"
4. 设置新密码后重启服务器

### Q9：如何查看服务器资源使用情况？

```bash
# 查看 CPU 和内存使用
top

# 查看磁盘使用
df -h

# 查看内存使用
free -h

# 查看 PM2 管理的进程资源
pm2 monit
```

---

> **最后提醒**：部署完成后，请务必修改数据库密码、设置防火墙、配置 HTTPS，确保系统安全。